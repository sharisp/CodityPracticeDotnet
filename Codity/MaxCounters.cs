using System;
using System.Collections.Generic;
using System.ComponentModel;
using System.Diagnostics.Metrics;
using System.Linq;
using System.Text;
using System.Threading.Tasks;
using static System.Runtime.InteropServices.JavaScript.JSType;

namespace Codity
{
    public class MaxCounters
    {
        /*  You have N counters, initially all set to 0. You are given an array of operations:
      • if A[i] = X, increase counter X by 1
      • if A[i] = N + 1, set all counters to the maximum value of any counter
  Write a function that returns the state of the counters after all operations.
  Solution(C#):
        */
        public int[] MaxCountersFunc(int N, int[] A)
        {
            //wait to implement
            return A;
        }
    }
